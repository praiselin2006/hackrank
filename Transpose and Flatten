import numpy



n, m = map(int, input().split())   

matrix = []

for _ in range(n):
    row = list(map(int, input().split()))
    matrix.append(row)

arr = numpy.array(matrix)
print(arr.transpose())
print(arr.flatten())
