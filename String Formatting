 w = len(f"{number:b}")
  for each in range(1, number+1):
    print(f"{each:d}".rjust(w),
          f"{each:o}".rjust(w),
          f"{each:x}".upper().rjust(w),
          f"{each:b}".rjust(w))def print_formatted(number):
    # your code goes here
